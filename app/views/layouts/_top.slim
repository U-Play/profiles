/div.contain-to-grid
  /nav.top-bar.fixed
      /ul.title-area
        /li.name
          /h1
            /a UPlayPro Files
      /ul.right
        /li.divider
        /li
          /- if user_signed_in?
            /= link_to 'Log out', destroy_user_session_path, method: :delete
          /- else
            /= link_to 'Log in', new_user_session_path
